import 'package:flutter/material.dart';

class GroundRules extends StatelessWidget {
  const GroundRules({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        //show rules
        );
  }
}
